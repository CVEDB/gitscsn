# Completion

```bash
Generate the autocompletion script for gitscan for the specified shell.
See each sub-command's help for details on how to use the generated script.

Usage:
  gitscan completion [command]

Available Commands:
  bash        Generate the autocompletion script for bash
  fish        Generate the autocompletion script for fish
  powershell  Generate the autocompletion script for powershell
  zsh         Generate the autocompletion script for zsh

Flags:
  -h, --help   help for completion

Global Flags:
      --cache-dir string          cache directory (default "/Users/didier/Library/Caches/gitscan")
  -c, --config string             config path (default "gitscan.yaml")
  -d, --debug                     debug mode
      --generate-default-config   write the default config to gitscan-default.yaml
      --insecure                  allow insecure server connections when using TLS
  -q, --quiet                     suppress progress bar and log output
      --timeout duration          timeout (default 5m0s)
  -v, --version                   show version
```
